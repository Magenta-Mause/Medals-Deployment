services:
  nginx:
    image: nginx:1.27.3
    ports:
      - "3005:3005"
    volumes:
      - "./nginx/default.conf:/etc/nginx/conf.d/default.conf"

  frontend:
    image: ecofreshkaese/medals-frontend

  backend:
    image: ecofreshkaese/medals-backend

services:
  nginx:
    image: medals-frontend
    ports:
      - "80:80"
    volumes:
      - "./nginx/default.conf:/etc/nginx/conf.d/default.conf"

  backend:
    image: ecofreshkaese/medals-backend
